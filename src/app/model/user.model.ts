export interface UserRegister {
  phone: string;
  password: string;
}

export interface UserLogin {
  phone: string;
  password: string;
}

export interface UserData {
  name: string;
  token: string;
  email: string;
  url: string;
  phoneNumber: string;
}
